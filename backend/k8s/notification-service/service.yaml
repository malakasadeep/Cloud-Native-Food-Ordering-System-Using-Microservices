apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: food-delivery-system
spec:
  selector:
    app: notification-service
  ports:
  - port: 5005
    targetPort: 5005
  type: ClusterIP
