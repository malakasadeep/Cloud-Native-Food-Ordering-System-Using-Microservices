apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: food-delivery-system
spec:
  selector:
    app: api-gateway
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer # Exposing the API gateway externally
