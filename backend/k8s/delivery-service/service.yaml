apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: food-delivery-system
spec:
  selector:
    app: delivery-service
  ports:
    - port: 5004
      targetPort: 5004
  type: ClusterIP
